---
interface Props {
  titulo: string;
  modelo: string;
  voltaje: string;
  amperaje: string;
  rangoAh: string;
  imageSrc?: string;
  imageAlt?: string;
  categoria?: string;
  hrefCotizar?: string;
  ctaText?: string;
}

const {
  titulo,
  modelo,
  voltaje,
  amperaje,
  rangoAh,
  imageSrc,
  imageAlt,
  categoria = "PBM Single Phase",
  hrefCotizar,
  ctaText = "Get Quote",
} = Astro.props as Props;

const whatsappNumber = "15626706851";
const whatsappMessage = `Hello GIBS, I want a quote for ${titulo} (${modelo})`;
const hrefDefault = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(whatsappMessage)}`;
const hrefFinal = hrefCotizar ?? hrefDefault;
---

<article
  class:list={[
    "relative",
    "group",
    "flex",
    "flex-col",
    "h-full",
    "w-full",
    "max-w-[23rem]",
    "rounded-2xl",
    "border",
    "border-emerald-200/20",
    "bg-white/6",
    "backdrop-blur-sm",
    "p-5",
    "shadow-[0_16px_35px_rgba(0,0,0,0.35)]",
    "transition-transform",
    "duration-300",
    "group-hover:-translate-y-1",
  ]}
>
  <p
    class:list={[
      "mb-3",
      "w-fit",
      "rounded-full",
      "border",
      "border-emerald-200/35",
      "bg-emerald-300/10",
      "px-3",
      "py-1",
      "text-xs",
      "font-semibold",
      "uppercase",
      "tracking-[0.16em]",
      "text-emerald-100",
    ]}
  >
    {categoria}
  </p>

  <h3
    class:list={[
      "text-white",
      "text-xl",
      "font-bold",
      "leading-snug",
      "sm:text-2xl",
    ]}
  >
    {titulo}
  </h3>
  <p class:list={["mt-1", "text-emerald-100/80", "text-sm"]}>Model: {modelo}</p>

  {imageSrc && (
    <div
      class:list={[
        "mt-5",
        "overflow-hidden",
        "rounded-2xl",
        "border",
        "border-white/15",
      ]}
    >
      <img
        src={imageSrc}
        alt={imageAlt ?? titulo}
        class:list={[
          "h-44",
          "w-full",
          "object-cover",
          "transition-transform",
          "duration-500",
          "group-hover:scale-105",
        ]}
      />
    </div>
  )}

  <ul
    class:list={[
      "mt-5",
      "space-y-2",
      "text-sm",
      "text-emerald-50/90",
      "leading-relaxed",
      "sm:text-base",
    ]}
  >
    <li
      class:list={[
        "flex",
        "items-center",
        "justify-between",
        "gap-3",
        "rounded-xl",
        "border",
        "border-white/10",
        "bg-black/10",
        "px-3",
        "py-2",
      ]}
    >
      <span class:list={["font-semibold", "text-emerald-100"]}>Voltage</span>
      <span>{voltaje}</span>
    </li>
    <li
      class:list={[
        "flex",
        "items-center",
        "justify-between",
        "gap-3",
        "rounded-xl",
        "border",
        "border-white/10",
        "bg-black/10",
        "px-3",
        "py-2",
      ]}
    >
      <span class:list={["font-semibold", "text-emerald-100"]}>Amperage</span>
      <span>{amperaje}</span>
    </li>
    <li
      class:list={[
        "flex",
        "items-center",
        "justify-between",
        "gap-3",
        "rounded-xl",
        "border",
        "border-white/10",
        "bg-black/10",
        "px-3",
        "py-2",
      ]}
    >
      <span class:list={["font-semibold", "text-emerald-100"]}>Battery Range</span>
      <span>{rangoAh} Ah</span>
    </li>
  </ul>

  <a
    href={hrefFinal}
    target="_blank"
    rel="noopener noreferrer"
    class:list={[
      "inline-flex",
      "items-center",
      "justify-center",
      "mx-auto",
      "mt-auto",
      "pt-4",
      "px-6",
      "py-2.5",
      "rounded-full",
      "bg-white",
      "text-green-900",
      "font-semibold",
      "tracking-wide",
      "shadow-md",
      "hover:bg-emerald-100",
      "transition",
      "duration-200",
      "text-sm",
      "sm:text-base",
    ]}
  >
    {ctaText}
  </a>
</article>
